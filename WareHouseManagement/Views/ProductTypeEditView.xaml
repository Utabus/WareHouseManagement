<Window x:Class="WareHouseManagement.Views.ProductTypeEditView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        Title="{Binding Title}" 
        Height="350" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Type Code -->
        <TextBlock Text="Mã loại:" VerticalAlignment="Center" Grid.Row="0" Grid.Column="0" Margin="0,5"/>
        <hc:TextBox Text="{Binding ProductType.TypeCode}" Grid.Row="0" Grid.Column="1" Margin="0,5"/>

        <!-- Type Name -->
        <TextBlock Text="Tên loại:" VerticalAlignment="Center" Grid.Row="1" Grid.Column="0" Margin="0,5"/>
        <hc:TextBox Text="{Binding ProductType.TypeName}" Grid.Row="1" Grid.Column="1" Margin="0,5"/>

        <!-- Description -->
        <TextBlock Text="Mô tả:" VerticalAlignment="Top" Grid.Row="2" Grid.Column="0" Margin="0,5"/>
        <hc:TextBox Text="{Binding ProductType.Description}" Grid.Row="2" Grid.Column="1" Margin="0,5" Height="60" AcceptsReturn="True" TextWrapping="Wrap"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <!-- Lưu -->
            <Button Content="Lưu" Width="100" Margin="0,0,10,0" 
        Command="{Binding SaveCommand}" 
        CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>

            <!-- Hủy -->
            <Button Content="Hủy" Width="100" 
        Command="{Binding CancelCommand}" 
        CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>

        </StackPanel>
    </Grid>
</Window>
